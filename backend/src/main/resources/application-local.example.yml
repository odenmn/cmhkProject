spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cmhk?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: your_mysql_password
  data:
    redis:
      host: your_redis_host
      port: 6379
      database: 0
      password: your_redis_password

cmhk:
  auth:
    token-secret: replace_with_a_random_local_token_secret
  admin:
    username: admin
    password: replace_with_a_strong_admin_password
    token-secret: replace_with_an_independent_random_secret
    access-token-ttl-hours: 12
