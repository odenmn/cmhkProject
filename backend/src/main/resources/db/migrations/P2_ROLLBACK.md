# P2 回滚兼容说明

- 不删除已写入的客户跟进、订单状态历史或渠道产品政策数据。
- 应用回滚时可停止使用新增表和字段；历史订单的标准状态保留，避免反向猜测中文自由文本。
- 若确需回退状态映射，必须先导出订单 ID、当前状态和状态历史，再由人工审核执行。
- P2 迁移执行前必须保留完整数据库备份；数据库级回退优先恢复该备份到独立数据库核对，不在真实库直接删除新增列或表。
- `mobile_plan_order` 保存了套餐价格和规则快照，产品维护和回滚均不得用当前套餐表反向覆盖历史订单。
