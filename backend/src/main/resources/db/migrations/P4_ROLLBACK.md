# P4 回滚兼容说明

1. 回滚前停止任务中心写操作，并备份 `operation_task` 与 `operation_task_history`。
2. 应用回滚到 P3 版本后可停止读取任务表；任务本身只记录内部处理，不会反向修改客户、订单、ICCID、推荐号码或 UMALL 状态。
3. 不建议在真实库直接删除任务表，因为会丢失运营处理历史；如必须恢复到 P3 结构，应先在独立备份库核验后再由人工执行。
4. `operation_log` 保留，不因 P4 回滚而清理。
