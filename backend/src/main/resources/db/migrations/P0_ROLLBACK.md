# P0 回滚与兼容说明

P0 不执行真实数据库的 DDL 或 DML，不删除 `mobile_plan_order.id_no`，也不清理任何历史数据，因此数据库无需执行反向 SQL。

如需回滚应用代码：

1. 恢复 H5 的 `idNo` 输入和请求类型。
2. 恢复后端请求 DTO 与 Service 写入逻辑。
3. 移除订单实体上的响应忽略配置。
4. 重新执行后端测试、两个前端类型检查与构建。

回滚应用代码不会修改数据库中的历史 `id_no`。如未来需要清理历史证件号码，必须重新统计影响数量、备份数据并取得用户明确确认，不属于 P0 回滚范围。
